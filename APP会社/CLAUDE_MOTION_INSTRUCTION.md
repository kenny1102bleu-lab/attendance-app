# 🤖 クロード・コード専用：ハル（HAL）モーション・仕草自動切り出し指示書

クロード・コード（Claude Code）へ：
以下の手順に従って、社長のダウンロードフォルダ（`Downloads`）にある動画から、特徴的な表情・モーション（仕草）のサンプル（３種類 × ２人分 ＝ 計６つ）を自動で切り出し、パブリック（`public/`）フォルダに保存してください。

---

## 🎯 実行ステップ

### ステップ １：プロジェクト環境の確認
以下の作業ディレクトリに移動していることを確認してください：
👉 `c:\Users\kenny\.gemini\antigravity\scratch\attendance_app\APP会社`

### ステップ ２：モーション切り出しスクリプトの実行
ウィンドウズのパワーシェル制限をすり抜けるため、通常の `node` コマンドに絶対パスを指定して `extract_motions.js` を実行し、動画切り出しプロセスを起動してください。

以下のコマンドを実行してください：
```bash
node "c:\Users\kenny\.gemini\antigravity\scratch\attendance_app\APP会社\extract_motions.js"
```

### ステップ ３：出力の確認
パブリック（`public/`）フォルダ内に、以下の６つのエムピーフォー（`MP4`）動画が正常に書き出されたことを確認してください：
1.  `winter_motion_01.mp4`（あまあま小首かしげトーク）
2.  `winter_motion_02.mp4`（無邪気な両手パタパタはにかみ笑顔）
3.  `winter_motion_03.mp4`（深く優しくうなずく共感ポーズ）
4.  `ayaka_motion_01.mp4`（髪を耳にかける大人の色気仕草）
5.  `ayaka_motion_02.mp4`（おっとり女神の微笑み）
6.  `ayaka_motion_03.mp4`（優しく肯く仕草）

確認が完了したら、社長へ日本語で無事に切り出しが完了した旨を報告してください。
